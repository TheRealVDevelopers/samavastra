{
    "name": "samavastra",
    "version": "1.0.0",
    "description": "Samavastra ERP — School Uniform Management System",
    "scripts": {
        "dev": "npm run dev --prefix frontend",
        "build": "npm run build --prefix frontend",
        "preview": "npm run preview --prefix frontend",
        "install": "npm install --prefix frontend"
    }
}